# Hypothesis database — track the examples dir (counterexample replay) but not the rest
.hypothesis/unicode_data/
.hypothesis/*.json

# Python build artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# Virtual environments
.venv/
venv/

# uv lockfile artefacts (committed separately if uv is adopted)
# uv.lock is intentionally excluded from gitignore — commit it when generated
