# DOG-08 — dogfood marker file

This PR exists to exercise the merge-queue handler's `no opted-in path` rejection branch (RFC §6, plan 03-06 cmd_handle.py). Driver leaves the PR in place per CONTEXT.md D-04.
